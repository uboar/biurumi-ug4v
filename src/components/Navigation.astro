---
import { Menu, X } from '@lucide/astro';

type Props = {
  currentPath: string;
};

const { currentPath } = Astro.props;
const links = [
  { href: '/profile/', label: 'Profile' },
  { href: '/world/', label: 'World' },
  { href: '/story/', label: 'Story' },
  { href: '/about/', label: 'About' },
];

const normalize = (path: string) => (path.endsWith('/') ? path : `${path}/`);
const activePath = normalize(currentPath);
---

<header class="site-header">
  <a class="brand-mark" href="/" aria-label="PROJECT BIURUMI home">
    <img src="/assets/brand/typo.svg" alt="PROJECT BIURUMI" />
  </a>

  <p class="event-strip" aria-label="site status">
    <span>Character file</span>
    <span>Draft shell</span>
  </p>

  <nav class="desktop-nav" aria-label="主要ナビゲーション">
    {
      links.map((link) => (
        <a
          href={link.href}
          aria-current={activePath === normalize(link.href) ? 'page' : undefined}
        >
          {link.label}
        </a>
      ))
    }
  </nav>

  <details class="mobile-nav">
    <summary aria-label="メニューを開く">
      <Menu class="menu-icon menu-icon-open" size={22} strokeWidth={2.5} aria-hidden="true" />
      <X class="menu-icon menu-icon-close" size={22} strokeWidth={2.5} aria-hidden="true" />
    </summary>
    <nav aria-label="モバイルナビゲーション">
      {
        links.map((link) => (
          <a
            href={link.href}
            aria-current={activePath === normalize(link.href) ? 'page' : undefined}
          >
            {link.label}
          </a>
        ))
      }
    </nav>
  </details>
</header>
